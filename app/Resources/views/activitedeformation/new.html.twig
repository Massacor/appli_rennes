{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'une activité de formation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('activite_index', {'progid':program.id, 'modid':module.id, 'seqid':sequence.id}) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
