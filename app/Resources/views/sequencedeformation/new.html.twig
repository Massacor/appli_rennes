{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'une séquence de formation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('sequence_index', {'progid':program.id, 'modid':module.id}) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
